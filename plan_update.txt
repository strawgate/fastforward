1. Implement feedback from PR comments:
   - Change `max_file_duration_seconds` to `max_file_duration` as string (duration like "5s", "1m"). Oh wait, the comment says: "Use duration strings ("5s", "200ms", "1m") not _ms integers. If duration strings are too complex, use _secs integer suffix (Vector pattern). The _ms pattern is unique to memagent — no other collector uses it." Since we already used `max_file_duration_seconds`, maybe it's acceptable per the comment? No, "The `_ms` pattern is unique to memagent" and "use `_secs` integer suffix" implies we should rename to `max_file_duration_secs` OR use a duration string. Let's see if there is a `deserialize_duration` in memagent. The memory says "For parsing std::time::Duration from config strings (e.g., "5s" or "100ms"), logfwd-config uses custom deserialization helpers like deserialize_duration instead of external crates." But `grep deserialize_duration crates/logfwd-config/src` found nothing. Let me look closer at the comment.

Ah! The comment specifically mentions:
**Retry/batch fields**: Group into nested structs:
```yaml
retry:
  max_attempts: 10
  initial_interval: "1s"
  max_interval: "30s"
batch:
  max_bytes: 1048576
  timeout: "1s"
```

**No dead config**: Every config field must be wired to runtime behavior. If you cannot wire it, do not add it. `#[allow(dead_code)]` on config fields is unacceptable.

Wait, since `OutputType::Parquet` isn't fully implemented in `build_sink_factory`, are these fields "dead config"? If I can't wire them, I shouldn't add them. The prompt asked: "Fill out missing configuration options for the Parquet output in strawgate/memagent." But the PR reviewer says: "If you cannot wire it, do not add it. `#[allow(dead_code)]` on config fields is unacceptable."

Let me check where the Parquet output might be wired. Maybe it's not wired at all?
I didn't add `#[allow(dead_code)]`, but maybe rustc emitted a warning about them being unused, and since I used `#![deny(warnings)]` it failed? Let me check the rustc output.
Wait, my tests passed. I didn't see `dead_code` warnings. But maybe the reviewer is anticipating it or I missed it because it's a library?
Let's wire it or change it.
